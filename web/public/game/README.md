# Game build output

The Godot web export lands in this directory (`game/export_presets.cfg`
points the Web preset at `web/public/game/index.html`).

Build artifacts are gitignored; only this README is tracked. To produce a
build, see [docs/DEPLOYMENT.md](../../../docs/DEPLOYMENT.md).
